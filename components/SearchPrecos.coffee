define ['components/DispatcherMixin'], (DispatcherMixin) ->
  {style, img, button, div, span, a, p, hr, label, fieldset, legend, table, tr, th, td, h1, h2, h3, h4, form, input, textarea} = React.DOM

  return React.createClass
    render: (div {})
